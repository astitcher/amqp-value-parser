{
  "name": "amqp-value-parser",
  "build": { "dockerfile": "Dockerfile" },
  "features": {},
  "postCreateCommand": "cmake -S . -B build -G Ninja && cmake --build build",
  "customizations": {
    "vscode": {
      "extensions": [
        "ms-vscode.cpptools",
        "ms-vscode.cmake-tools"
      ]
    }
  },
  "remoteUser": "vscode"
}
